
    
        
            <div class="menambah-data-petugas-harian-parent">
                <div class="menambah-data-petugas">Menambah Data Jadwal Petugas</div>
                <div class="nim">Petugas</div>
                <input type="text" class="frame-child" placeholder="Masukkan nama petugas" />
                <div class="nama">Tanggal</div>
                <input type="datetime-local" class="frame-item" placeholder="Pilih tanggal dan waktu" />
                <div class="prodi">Waktu Shalat</div>
                <select class="frame-inner" placeholder="Pilih waktu shalat">
                    <option value="">-- Pilih Waktu Shalat --</option>
                    <option value="subuh">Subuh</option>
                    <option value="dzuhur">Dzuhur</option>
                    <option value="ashar">Ashar</option>
                    <option value="maghrib">Maghrib</option>
                    <option value="isya">Isya</option>
                </select>
                <div class="notlpon">Tugas</div>
                <input type="text" class="frame-child1" placeholder="Masukkan tugas" />
                
                
                
                
                <!-- You can remove the following elements as they seem unnecessary for the Jadwal Petugas form -->
                <!-- These may be placeholders or unnecessary elements based on the provided code -->
                <!-- Adjust the class names if needed based on your styling -->
                <button class="simpan btn btn-primary" onclick="simpan()">Simpan</button>
                <button class="batal btn btn-secondary">Batal</button>
            </div>

   


    <script>
        loadPetugas()


        function loadPetugas() {
            var hostname = "https://localhost:44307/"
            var url = hostname + "GetAllPetugasHarian"
            var method = "GET"

            $.ajax({
                url: url,
                method: method,
                contentType: "application/json",
                success: function (data) {
                    var list = data.data
                    list.forEach(function (item) {
                        var newOption = '<option value="' + item.nim + '">' + item.nama + '</option>';
                        $('#nim').append(newOption);
                    });
                },
            });
        }

        function simpan() {
            var nim = $("#nim").val();
            var tanggal = $("#tanggal").val();
            var waktu = $("#waktu").val();
            var tugas = $("#tugas").val();

            var hostname = "https://localhost:44307/";
            var url = hostname + "InsertJadwalPetugasHarian"; // Change the endpoint accordingly
            var method = "POST";
            var input = {
                nim: nim,
                tanggal: tanggal,
                waktu: waktu,
                tugas: tugas
            };
            var data = JSON.stringify(input);

            $.ajax({
                url: url,
                method: method,
                data: data,
                contentType: "application/json",
                success: function (data) {
                    alert("Data Jadwal Petugas Harian Berhasil Ditambahkan");
                },
                error: function () {
                    alert("Gagal menyimpan data Jadwal Petugas Harian.");
                }
            });
        }
    </script>
